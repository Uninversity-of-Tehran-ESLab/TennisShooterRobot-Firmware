After doing any changes to the files in the `ui` folder, run `GenerateFSData.sh` to generate the new UI data.

You need to have [inliner](https://github.com/remy/inliner) installed using `npm install -g inliner`.
